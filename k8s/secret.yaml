apiVersion: v1
kind: Secret
metadata:
  name: url-shortener-secrets
type: Opaque
stringData:
  # In a real production environment, you would never commit this file to git!
  # You would use a Secret Manager or encrypt it with tools like SealedSecrets.
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "postgres_secure_password123"
